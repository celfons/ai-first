# Copie para .env e preencha. NUNCA comite o .env (está no .gitignore).
# Chave da API da Anthropic — console.anthropic.com → Settings → API Keys → Create Key
ANTHROPIC_API_KEY=sk-ant-...

# Porta do WebSocket (opcional; default 8787)
PORT=8787
