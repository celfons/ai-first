{
  "name": "session-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Relay que segura a ANTHROPIC_API_KEY no backend e repassa eventos de uma sessão Managed Agents ao canvas BPMN via WebSocket.",
  "scripts": {
    "start": "node relay.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
